set title           " Display the title of the file
set number          " Line numbers
set showcmd         " Show the last command
set textwidth=80    " Standard
set tabstop=8       " Standard
set softtabstop=2   " Looks better
set shiftwidth=2
set smartindent     " Because manual indenting sucks
set background=dark " Comments look better
set hlsearch        " Highlight the search results
set incsearch       " Search as we go
set ignorecase      " Ignore the case when searching
set smartcase       " Unless we use upper case
set scrolloff=2     " Show 2 above/below the cursor when scrolling
set confirm         " Shows a confirm whenever writing/quitting fails
filetype plugin on  " Hilighting, and makefile detection
